type Mutation {
    createPlayList(input: PlayListInput!): PlayList,
    login(input: LoginInput!): LoginOutput
}

input PlayListInput {
    name: String!
}

input LoginInput {
    user: String!
    password: String!
}

type LoginOutput {
    user: String
    token: String
}